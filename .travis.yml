language: generic
sudo: required
services:
  - docker

before_install:
  - "docker build -t carlanders/docker-react -f Dockerfile.dev ."

script:
  - "docker run -e CI=true carlanders/docker-react npm run test"